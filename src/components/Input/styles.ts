import styled from "styled-components/native";
import { TextInput } from "react-native";

export const Conainer = styled(TextInput)`
    flex:1;
    
    min-height: 56px;
    max-height: 56px;

    background-color: #121214;
    color: #FFFF;

    border-radius: 6px;
    padding: 16px;
`;
